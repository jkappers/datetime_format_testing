<% if @interaction.errors.any? %>
  <% @interaction.errors.full_messages.each do |message| %>
    <%= message %> <br/>
  <% end %>
<% end %>

<%= form_for @interaction, as: :user, url: user_path(@interaction, id: @interaction.user.id), method: :put do |f| %>
  <%= f.text_field :name %>
  <br/>
  <%= f.text_field :birth_date %>
  <br/>
  <%= f.submit %>
<% end %>
